name: 🚀 CI/CD Pipeline - PetTrack Microservices

on:
  push:
    branches: [main]

env:
  TAG: latest
  APP_NAME: pettrack
  COMPOSE_FILE: docker-compose.yml

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # ================================
    # 🔹 Checkout del código
    # ================================
    - name: Checkout code
      uses: actions/checkout@v4

    # ================================
    # 🔐 Login a Azure
    # ================================
    - name: Azure login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # ================================
    # 🧩 Crear archivos .env desde los secrets
    # ================================
    - name: Create .env files from secrets
      run: |
        mkdir -p api-gateway auth-service pets-service appointment-service certs

        echo "${{ secrets.ENV_API_GATEWAY }}" > api-gateway/.env
        echo "${{ secrets.ENV_AUTH_SERVICE }}" > auth-service/.env
        echo "${{ secrets.ENV_PETS_SERVICE }}" > pets-service/.env
        echo "${{ secrets.ENV_APPOINTMENT_SERVICE }}" > appointment-service/.env

        mkdir -p ./certs
        echo "${{ secrets.AZURE_CA_CERT }}" > ./certs/BaltimoreCyberTrustRoot.pem

        echo "✅ .env files created for all microservices"

    # ================================
    # 🐳 Login al Azure Container Registry
    # ================================
    - name: Docker login to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server: ${{ secrets.ACR_LOGIN_SERVER }}
        username: ${{ secrets.ACR_USERNAME }}
        password: ${{ secrets.ACR_PASSWORD }}

    # ================================
    # ⚙️ Build de imágenes
    # ================================
    - name: Build Docker images
      run: docker compose -f $COMPOSE_FILE build

    # ================================
    # 📦 Push de imágenes al ACR
    # ================================
    - name: Push Docker images to Azure Container Registry
      run: docker compose -f $COMPOSE_FILE push

    # ================================
    # 🚀 (Opcional) Desplegar a Azure Web App for Containers
    # ================================
    - name: Deploy to Azure Web App for Containers
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.APP_NAME }}
        images: |
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.APP_NAME }}-api-gateway:${{ env.TAG }}
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.APP_NAME }}-auth-service:${{ env.TAG }}
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.APP_NAME }}-pets-service:${{ env.TAG }}
          ${{ env.ACR_LOGIN_SERVER }}/${{ env.APP_NAME }}-appointment-service:${{ env.TAG }}
